<Window x:Class="PerformanceMonitor.Darling.Viewer.MuteRulesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Manage Mute Rules"
        Height="500" Width="820"
        WindowStartupLocation="CenterOwner"
        Icon="EDD.ico"
        ResizeMode="CanResizeWithGrip"
        Background="{DynamicResource BackgroundLightBrush}"
        Foreground="{DynamicResource ForegroundBrush}">
    <!-- Mirrors Lite's ManageMuteRulesWindow (Lite/Windows/ManageMuteRulesWindow.xaml), dark-styled
         via the shared ViewerDarkTheme resource dictionary. Persists straight to Postgres via
         ViewerDataService (no in-memory MuteRuleService cache — the running service re-reads). -->
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="ViewerDarkTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Mute Rules" FontSize="18" FontWeight="Bold" HorizontalAlignment="Left" Margin="0,0,0,4"/>
        <TextBlock Grid.Row="1" Text="Muted alerts are still collected and logged, but suppressed from alert delivery (email and webhooks). Changes are written to the shared store and take effect on the service's next mute-rule load."
                   Foreground="{DynamicResource ForegroundMutedBrush}" TextWrapping="Wrap" HorizontalAlignment="Left" Margin="0,0,0,12"/>

        <Grid Grid.Row="2">
            <DataGrid x:Name="RulesGrid"
                      Style="{StaticResource DarkGrid}"
                      CanUserDeleteRows="False"
                      MouseDoubleClick="RulesGrid_MouseDoubleClick">
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="Enabled" Width="60">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding Enabled, UpdateSourceTrigger=PropertyChanged}"
                                          IsEnabled="{Binding RulesAreEditable, RelativeSource={RelativeSource AncestorType=Window}}"
                                          HorizontalAlignment="Center" VerticalAlignment="Center"
                                          Click="EnabledCheckBox_Click"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Metric" Binding="{Binding MetricName}" Width="130"/>
                    <DataGridTextColumn Header="Server" Binding="{Binding ServerName}" Width="130"/>
                    <DataGridTextColumn Header="Reason" Binding="{Binding Reason}" Width="*"/>
                    <DataGridTextColumn Header="Expires" Binding="{Binding ExpiresDisplay}" Width="140"/>
                </DataGrid.Columns>
            </DataGrid>
            <TextBlock x:Name="NoRulesMessage"
                       Text="No mute rules yet. Add one, or create one from an alert in the Alerts tab."
                       Foreground="{DynamicResource ForegroundMutedBrush}"
                       HorizontalAlignment="Center" VerticalAlignment="Center"
                       TextWrapping="Wrap" TextAlignment="Center" MaxWidth="420"
                       Visibility="Collapsed"/>
        </Grid>

        <StackPanel x:Name="ActionButtonsPanel" Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,12,0,0">
            <Button Content="_Add Rule..." Width="100" Height="28" Margin="0,0,8,0" Click="AddRule_Click"/>
            <Button Content="_Edit" Width="80" Height="28" Margin="0,0,8,0" Click="EditRule_Click"/>
            <Button Content="_Toggle" Width="80" Height="28" Margin="0,0,8,0" Click="ToggleRule_Click"/>
            <Button Content="_Delete" Width="80" Height="28" Margin="0,0,8,0" Click="DeleteRule_Click"/>
            <Button Content="_Purge Expired" Width="110" Height="28" Click="PurgeExpired_Click"/>
        </StackPanel>
        <TextBlock x:Name="ReadOnlyBanner" Grid.Row="3" HorizontalAlignment="Left" VerticalAlignment="Center"
                   Margin="0,12,0,0" Foreground="{DynamicResource WarningTextBrush}" TextWrapping="Wrap" MaxWidth="560" Visibility="Collapsed"
                   Text="Read-only connection (postgres.connectAs = &quot;viewer&quot;): mute rules can be viewed but not changed here. Set connectAs to &quot;admin&quot; and restart the viewer to edit them."/>
        <Button Grid.Row="3" Content="_Close" Width="80" Height="28" Margin="0,12,0,0"
                HorizontalAlignment="Right" Click="Close_Click" IsCancel="True"/>
    </Grid>
</Window>
